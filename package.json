{
  "name": "tristate-property-management",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "wrangler dev",
    "deploy": "npm run build && wrangler deploy",
    "check": "astro check",
    "db:local": "wrangler d1 execute tristate-db --local --file=./db/schema.sql",
    "db:remote": "wrangler d1 execute tristate-db --remote --file=./db/schema.sql",
    "db:migrate:auth": "wrangler d1 execute tristate-db --remote --file=./db/migrations/0001_auth.sql",
    "db:migrate:profiles": "wrangler d1 execute tristate-db --remote --file=./db/migrations/0002_profiles.sql",
    "types": "wrangler types"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^14.2.5",
    "@astrojs/rss": "^4.0.19",
    "@astrojs/sitemap": "^3.7.3",
    "@fontsource-variable/fraunces": "^5.3.0",
    "astro": "^7.2.9",
    "better-auth": "^1.7.3",
    "kysely": "^0.29.5",
    "kysely-d1": "^0.4.0",
    "zod": "^4.5.2"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@fontsource-variable/inter": "^5.3.0",
    "@fontsource/ibm-plex-mono": "^5.3.0",
    "typescript": "^6.0.3",
    "wrangler": "^4.127.1"
  }
}
